fn main() {
	let fixed = 45;
	println!("fixed: {}", fixed);

	let mut mutable = 100;
	println!("mutable: {}", mutable);
	mutable = 20;
	println!("mutable is now: {}", mutable);

	let int_thirty_two: i32 = 45;
	println!("a thirty-two bit integer: {}", int_thirty_two);
}
